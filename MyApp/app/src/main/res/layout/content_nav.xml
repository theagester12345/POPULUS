<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"

    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.chris.mcdelait.myapplication.NavActivity"
    tools:showIn="@layout/app_bar_nav"
     android:background="#99CCCC">


    <Button
        android:theme="@style/startButton"

        android:textAllCaps="false"
        android:fontFamily="gisha"
        android:textColor="#FFFFCC"
        android:textSize="9pt"
        android:layout_height="200px"
        android:layout_width="match_parent"
        android:text="Start A New Household Entry In the Same House"
        android:id="@+id/buttonStart"
        android:layout_column="4" />
    <Button
        android:layout_marginTop="44dp"

        android:theme="@style/startButton"

        android:textAllCaps="false"
        android:fontFamily="gisha"
        android:textColor="#FFFFCC"
        android:textSize="9pt"
        android:layout_height="200px"
         android:layout_width="match_parent"
        android:text="Start A New Household Entry In a New House"
        android:id="@+id/buttonStartNewHouse"
        android:layout_column="4"
        android:layout_below="@+id/buttonStart"
        android:layout_alignParentStart="true" />
</RelativeLayout>
